import 'package:flutter/cupertino.dart';

class SeparatorMain extends StatelessWidget {
  const SeparatorMain({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 25,
    );
  }
}

class SeparatorSmall extends StatelessWidget {
  const SeparatorSmall({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 10,
    );
  }
}
